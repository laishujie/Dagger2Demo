<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lai.dagger2demo">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".MyApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".di.AMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
       <!-- <activity android:name=".di.AMainActivity"/>-->
        <activity android:name=".di.BMainActivity"/>
       <!-- <activity android:name=".di2.Di2Activity"/>-->
        <activity android:name=".di2.Di2Activity2"/>
      <!--  <activity android:name=".di2.Di2Activity3"/>-->
        <activity android:name=".di2.Di2Activity4"/>
    </application>

</manifest>